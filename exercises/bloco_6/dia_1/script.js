import states from './states_of_Brasil'
const statesList = document.querySelector('#user-state')

console.log(states)

function createOptions (states) {
    for (let index = 0; index < states.length; index += 1) {
        const state = document.createElement('option');
        option
        
    }
}